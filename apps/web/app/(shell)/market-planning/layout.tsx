import { MarketPlanningShell } from "../../../components/market-planning-shell";

export default function MarketPlanningLayout({ children }: { children: React.ReactNode }) {
  return <MarketPlanningShell>{children}</MarketPlanningShell>;
}
